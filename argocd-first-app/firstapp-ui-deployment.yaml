apiVersion: apps/v1
kind: Deployment
metadata:
  name: firstapp-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: firstapp-ui
  template:
    metadata:
      labels:
        app: firstapp-ui
    spec:
      containers:
        - image: gcr.io/google-samples/gb-frontend:v5
          name: firstapp-ui
          ports:
            - containerPort: 80