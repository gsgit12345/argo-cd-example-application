apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-firstapp-ui-base.fullname" . }}
  labels:
    {{- include "helm-firstapp-ui-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-firstapp-ui-base.selectorLabels" . | nindent 4 }}
